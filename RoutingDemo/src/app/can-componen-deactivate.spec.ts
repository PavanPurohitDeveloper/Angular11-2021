import { CanComponenDeactivate } from './can-componen-deactivate';

describe('CanComponenDeactivate', () => {
  it('should create an instance', () => {
    expect(new CanComponenDeactivate()).toBeTruthy();
  });
});
